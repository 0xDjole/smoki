import type { NavPath, NavPaths } from '../types';
export declare const getScreenPath: (navigationPath: NavPath[], parentPath: boolean) => NavPaths;
export declare const getPathFromUrl: (url: any) => any;
